<main>
    <h2>HOME</h2>

    {{#each book}}
    <div class="book">
        <h3>Título: {{this.title}}</h3>
        <p>Quantidade de páginas: {{this.pageqty}}</p>
        <a href="/book/{{this.id_book}}">Ver mais</a>
    </div>
    {{/each}}
</main>